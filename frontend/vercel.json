{
  "$comment": "Vercel deployment config. Replace <your-api-service> with your Render service hostname after step 4 (e.g. carmarket-api.onrender.com). Rewrites are evaluated in order — the /api and /uploads rules MUST stay above the /index.html catch-all.",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://<your-api-service>.onrender.com/api/$1" },
    { "source": "/uploads/(.*)", "destination": "https://<your-api-service>.onrender.com/uploads/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
